# Auditor レポート — PR #24

**日時**: 2026-05-24
**PR**: feat: 予約ページUI改善（ヘッダー・便選択・チャーター・釣り物統一）
**総合判定**: 🟡 Yellow — 人間確認推奨

## Architecture Auditor

**判定**: 🟡 YELLOW
**概要**: 全体的に問題は軽微だが、CSS変数未使用のハードコード色追加・font-weight:500の確認・contacts APIのvessel_id絞り込み欠如などが懸念される

- [YELLOW] getBinBadge関数でbg/colorにCSS変数（var(--ocean)等）を使わずハードコード色（'#E8F4FD', '#1B2A4A' 等）を新規追加している。UI Design Auditorルール（CSS変数未使用）に抵触する可能性がある。 (src/app/reserve/[vesselId]/page.tsx)
- [YELLOW] カレンダーセル内のスタイル（cellBg '#F3F4F6', '#FFFFFF', dateColor '#9CA3AF' 等）もCSS変数ではなくハードコード色を使用しており、ダークモード対応含めCSS変数化の検討が推奨される。 (src/app/reserve/[vesselId]/page.tsx)
- [YELLOW] 新設の /api/contacts/route.ts では vessel_id を受け取って INSERT しているが、vessel_id の存在確認や権限チェックがなく、任意の vessel_id にデータを書き込める。不正なvessel_idへの書き込みリスクがある。 (src/app/api/contacts/route.ts)
- [YELLOW] チャーター問い合わせの送信処理がsupabaseクライアント直接呼び出しからfetch('/api/contacts')に変更されたが、Client Component側でsupabaseを直接使うパターンが他箇所（bookings取得等）と混在しており一貫性がない。 (src/app/reserve/[vesselId]/page.tsx)
- [YELLOW] font-weight: 500 が複数箇所で使われている（例: カレンダー日付表示）。ルール上 400/500 は許可されているが、既存コードとの統一確認を推奨。 (src/app/reserve/[vesselId]/page.tsx)
- [YELLOW] lowRemaining判定でカレンダーに残席数を数字表示する新UXが追加されているが、FigmaにないUIの追加に該当しないか確認が必要。 (src/app/reserve/[vesselId]/page.tsx)

## UX Auditor

**判定**: 🟡 YELLOW
**概要**: レスポンスのパースに失敗: ```json
{
  "verdict": "yellow",
  "summary": "全体的に問題は少ないが、IT用語の露出・タップ対象サイズ未確認・ハードコードカラーの多用・contacts

指摘事項なし
